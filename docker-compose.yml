version: "3.8"

services:
  zebra-discovery:
    image: dockerregistry.icomb.place/zebra-discovery:latest
    container_name: zebra-discovery
    network_mode: host
    environment:
      LAN_IP: ${LAN_IP:-192.168.16.4}
    restart: unless-stopped
